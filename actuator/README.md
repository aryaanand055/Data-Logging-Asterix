# Actuator

This folder is reserved for the actuator sensor/logger entrypoint.

- Shared database: `sensor_data.db` at the repo root
- Target table: `sensor_actuator`
- Dashboard: `dashboard/server.py`
