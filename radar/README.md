# Radar

Radar data will be ingested into the shared repository-level SQLite database at `sensor_data.db` once the CAN frame format is wired in.

- Target table: `sensor_radar`
- Planned transport: CAN
- Dashboard: `dashboard/server.py`
